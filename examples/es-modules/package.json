{
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server --open local --files \"./*.html,./*.css,../../dist/**/*\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "browser-sync": "^2.27.7"
  }
}
