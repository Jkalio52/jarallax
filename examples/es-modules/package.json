{
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server --open local --files \"./*.html,./*.css,../../dist/**/*\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}
